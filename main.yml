
---
# Exercise for setting up a web server in an EC
- name: Exercise for setting up a web server in an EC2 
  # hosts: ubuntu
  # Use the public IP if the VM is already created
  hosts: 204.236.196.150
  user: ec2-user
  become: true
  become_method: sudo
  become_user: root
  roles:
   - setup
